{
  "name": "low-code",
  "private": true,
  "version": "0.0.1",
  "description": "low code",
  "scripts": {
    "create": "cd packages && create-toolpad-app"
  },
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "create-toolpad-app": "^0.2.0"
  }
}
